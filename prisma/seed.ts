// Seed data lands with the auth phase (ROADMAP.md, Phase 1): a demo user,
// workspace and document tree. Wired into `npm run db:seed` from day one so
// the script location is stable.
console.log('No seed data yet — see ROADMAP.md Phase 1.');
